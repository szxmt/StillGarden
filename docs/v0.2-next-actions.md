# Lunette V0.2 Next Actions

更新时间：2026-06-30

本文件记录当前拆分后的下一步执行口径。当前阶段已进入不带新业务的重构纯净期；在 R1-R4 完成前，SQLite、打字机效果、Archive 编辑、自动触发器等新功能全部冻结。

## 当前结构状态

- 运行基准：`frontend/lunatte-v0.2`。
- 启动脚本：`frontend/lunatte-v0.2/start-stillgarden.bat` 和 `start-stillgarden.ps1`，已确认不会指向旧 `frontend/stillgarden-prototype-v0.1`。
- 数据路径：`D:\A月亮啊\lunette-v0.2\sessions\prototype`，不写 V0.1 原始数据。
- CSS：`styles.css` 是入口，模块已拆到 `css/`。
- JS：`script.js` 已降级为 bootstrapping 入口；复用底层在 `shared/`，Web DOM controller 在 `frontend/lunatte-v0.2/web/`。
- 后端：`server.py` 已降级为 HTTP/static 启动入口；业务逻辑在 `services/`，provider 请求在 `providers/`，API path 分发在 `routes/`。

## 当前优先级总令

当前唯一 feature 是“结构债务清算”。这不是边做功能边拆，也不是按未来功能触发拆分；本阶段必须先完成全栈结构重构，再恢复新业务开发。

必须在任何新功能前完成：

1. R1：JS API client 与 domain state 层拆分。
2. R2：页面 controller 层拆分，把 `script.js` 降级为 bootstrapping。
3. R3：Python service 层与 routes 层拆分，消灭单体 `server.py`。
4. R4：视图层最后清理，结构化 `index.html`，拆分超过 1000 行的 CSS 模块。

新功能冻结范围：

- SQLite / FTS5。
- 打字机效果。
- Archive 编辑 / 撤回 / 删除。
- Moments 自动触发器。
- Wake 调度器。
- 人格 / RAG 新行为。
- 任何改变 UI 或业务结果的功能。

重构期间的绝对准则：

- 不改变业务逻辑。
- 不改变 API 路径、请求 body、返回格式。
- 不改变 UI 视觉和交互。
- 不改变数据路径和 JSONL / localStorage 格式。
- 每个 Goal 完成后必须独立验证和提交。

## 已完成的拆分基线

### 1. V0.2 基线验证清单

为什么先做：

- V0.2 已经从稳定 V0.1 初始化，后续任何拆分前必须先确认当前基线可运行。
- 没有基线验证，CSS/JS/后端拆分后很难判断是新问题还是旧问题。

状态：已完成。后续每次功能开发仍沿用这套检查。

涉及文件：

- `frontend/lunatte-v0.2/README.md`
- `frontend/lunatte-v0.2/index.html`
- `frontend/lunatte-v0.2/script.js`
- `frontend/lunatte-v0.2/server.py`
- `frontend/lunatte-v0.2/start-stillgarden.bat`
- `frontend/lunatte-v0.2/start-stillgarden.ps1`
- `sessions/prototype/`

不允许动哪些文件：

- 不改 `index.html`
- 不改 `styles.css`
- 不改 `script.js`
- 不改 `server.py`
- 不改 `sessions/prototype` 数据内容
- 不改 V0.1 原目录 `D:\A月亮啊\memory-vault-starter`

怎么测试：

- 跑 `node --check frontend/lunatte-v0.2/script.js`
- 跑 `python -m py_compile frontend/lunatte-v0.2/server.py`
- 启动 V0.2 本地服务，只使用 V0.2 路径。
- 手动检查 Home、Chat、Archive、More 当前状态、Moments、Timeline 是否能打开。
- 确认服务灯指向当前 V0.2 服务，不是旧服务。

成功标准：

- 静态检查通过。
- 原型能启动。
- 不写 V0.1 原始数据。
- 形成一份可重复执行的基线验证清单。

### 2. CSS 第一轮拆分

为什么先做：

- `styles.css` 是当前 UI 的单体入口，后续必须拆，但不能盲拆。
- AGY 已经有部分 CSS 拆分，可作为参考，但不能直接合并。

状态：已完成第一轮。

涉及文件：

- `frontend/lunatte-v0.2/styles.css`
- `frontend/lunatte-v0.2/css/00-tokens.css`
- `frontend/lunatte-v0.2/css/00-foundation.css`
- `frontend/lunatte-v0.2/css/10-home-rooms.css`
- `frontend/lunatte-v0.2/css/20-chat.css`
- `frontend/lunatte-v0.2/css/21-profile-dialogs.css`
- `frontend/lunatte-v0.2/css/22-chat-search.css`
- `frontend/lunatte-v0.2/css/30-archive-timeline.css`
- `frontend/lunatte-v0.2/css/40-subpages-settings.css`
- `frontend/lunatte-v0.2/css/50-moments-wake.css`
- `frontend/lunatte-v0.2/css/60-config-dock-responsive.css`

当前结论：

- CSS 第一轮已完成，但视图层清理尚未完成。
- 后续主题化优先改 token，不为每套主题重写页面 CSS。
- `20-chat-profile-dialogs.css` 已在 R4 拆分为 chat、profile/dialog、chat-search 三个模块；后续主题化继续优先改 token，不复制页面 CSS。

### 3. JS 第一轮拆分

为什么做：

- `script.js` 负责状态、渲染、事件、API、数据写入，后续拆分风险高。
- 先把 Web/App 都可能复用的底层逻辑从 DOM 入口里拿出来。

状态：已完成第一轮。

涉及文件：

- `frontend/lunatte-v0.2/script.js`
- `frontend/lunatte-v0.2/shared/lunatte-core.js`

当前结论：

- JS 第一轮底层抽离已完成，但不能视为最终收口；`script.js` 仍然把 API 请求、浏览器状态、页面状态和 DOM 渲染混在一起。
- 当前阶段应把“API 层 + 状态层重构”作为独立 feature，在没有新业务功能干扰的纯净期完成。
- `shared/lunatte-core.js` 放通用 token、配置、请求封装、状态读写、格式化和纯数据 helper。
- DOM 渲染、事件绑定、页面切换目前仍在 `script.js`，这是现状，不是允许长期保留；R1 完成后必须无缝进入 R2 controller 拆分。

### 4. 后端第一轮拆分

为什么做：

- `server.py` 同时承担配置、路径、存储、routes 和业务逻辑，继续堆功能风险高。
- 本轮优先抽出稳定底层，避免继续无限小刀。

状态：已完成第一轮。

涉及文件：

- `frontend/lunatte-v0.2/server.py`
- `frontend/lunatte-v0.2/server_config.py`
- `frontend/lunatte-v0.2/server_storage.py`

当前结论：

- 后端底层拆分已完成，但后端不能收口。
- `server_config.py` 负责路径、默认配置、常量和 V0.2 数据边界。
- `server_storage.py` 负责 JSON/JSONL、secrets、profile assets、prototype 图片资产和统一 append。
- 复杂 routes、provider 调用、moments/timeline 业务逻辑仍堆在 `server.py`，这是本轮必须清算的结构债务；R2 完成后必须无缝进入 R3。

## Goal R1：JS API / 状态层纯净期拆分

本阶段把“重构”本身作为唯一 feature。目标不是加新功能，而是在稳定 V0.2 基线下，把 API 请求层和状态管理层从 `script.js` 干净抽离出来。任何业务行为、接口路径、返回格式、UI 表现、数据写入路径都不能改变。

### Goal 名称

```text
Goal R1：拆出 Lunette Web API client 与 domain state 层
```

### 为什么现在做

- `script.js` 当前约 3900 行，虽然已经比最初变小，但仍承担 API 请求、状态管理、业务流程、DOM 渲染、事件绑定。
- 这个体量下继续加功能会让 blast radius 过大，改一个小功能也可能牵连 Chat / Moments / Archive / Timeline / More。
- 当前 V0.2 基线已经验证稳定，没有新功能混入；此时只做结构拆分，最容易判断问题来自哪里。
- 如果等到新功能开发时边做边拆，业务 bug 和重构 bug 会混在一起，排错成本更高。

### 绝对准则

- 不改变业务逻辑。
- 不改变 API 路径。
- 不改变 API 请求 body 和响应处理结果。
- 不改变 DOM 结构。
- 不改变 CSS。
- 不改变 UI 视觉和交互。
- 不改变 `sessions/prototype` 数据。
- 不改变 localStorage key 名称和数据格式。
- 不引入构建工具。
- 不引入框架。
- 不把 Web DOM 逻辑塞进 `shared/`。

### 允许修改

- `frontend/lunatte-v0.2/index.html`：只允许增加新 JS 文件加载顺序。
- `frontend/lunatte-v0.2/script.js`：只允许把 API 和状态逻辑替换成新模块调用。
- `frontend/lunatte-v0.2/shared/lunatte-core.js`：只允许移动或复用纯 helper，不改变现有对外行为。
- 新增 `frontend/lunatte-v0.2/shared/lunatte-api.js`。
- 新增 `frontend/lunatte-v0.2/shared/lunatte-state.js`。
- 必要时更新 `frontend/lunatte-v0.2/README.md` 和本文档。

### 禁止修改

- 不改 `server.py`、`server_config.py`、`server_storage.py`。
- 不改 `styles.css` 和 `css/`。
- 不改 `sessions/prototype/`。
- 不改 `preview-desktop.png`、`preview-mobile.png`。
- 不改原始导出文件和旧记忆库源文件。
- 不创建多余 md。
- 不在仓库外创建文件。

### 模块目标

#### `shared/lunatte-api.js`

职责：

- 统一管理前端调用后端的 API client。
- 包装所有 `/api/*` 请求。
- 对外暴露语义化函数，而不是让页面层直接拼 path。

建议暴露：

- `getHealth()`
- `getStats()`
- `getConfig()`
- `saveConfig(payload)`
- `getSession(room, options)`
- `saveSessionMessage(room, text)`
- `buildChatPackage(room, draftRecord)`
- `callProviderModel(room, draftRecord)`
- `callAimasAgent(draftRecord)`
- `getMoments(options)`
- `createMoment(payload)`
- `updateMoment(payload)`
- `getTimeline(options)`
- `getWakeInbox()`
- `createWakeDraft(payload)`
- `updateWakeDraft(id, action)`
- `searchArchive(payload)`
- `getConfirmedMemory()`
- `confirmMemory(payload)`
- `refreshMemoryIndex()`
- `saveProfileAsset(payload)`
- `clearProfileAssets(payload)`

要求：

- 内部可以复用现有 `fetchJson`。
- 函数返回值必须与现在 `script.js` 直接调用时一致。
- 不在 API 层做 toast、DOM、渲染、按钮状态。
- 不吞掉异常；错误处理口径保持现有页面行为。

#### `shared/lunatte-state.js`

职责：

- 统一管理浏览器本地状态和纯状态计算。
- localStorage key 名称集中，但不改 key。
- 页面层只调用 state 函数，不直接散落拼 key。

建议暴露：

- chat prefs：`readChatPrefs()`、`writeChatPrefs(prefs)`、`chatPrefsFor(room)`、`updateChatPrefs(room, patch)`。
- browser drafts：`readBrowserDrafts(room)`、`writeBrowserDraft(room, text)`、`writeBrowserEntry(room, record)`。
- profile assets fallback：`readProfileAssets()`、`writeProfileAssets(assets)`、`profileAssetFor(room)`。
- moments fallback：`readBrowserMoments()`、`writeBrowserMoments(entries)`、`readMomentDraft()`、`writeMomentDraft(draft)`、`clearMomentDraft()`。
- wake fallback：`readBrowserWakeInbox()`、`writeBrowserWakeDraft(record)`、`updateBrowserWakeDraft(id, action)`。
- timeline state：筛选、分页和展开状态的纯计算 helper。

要求：

- 不读写 DOM。
- 不做 HTML 拼接。
- 不做 toast。
- 不调用后端 API。
- 不改变 localStorage 数据格式。

### 执行顺序

#### Phase 0：基线确认

状态：已完成。

只读检查：

- `git status --short --untracked-files=all`
- `node --check frontend\lunatte-v0.2\script.js`
- `python -m py_compile frontend\lunatte-v0.2\server_config.py frontend\lunatte-v0.2\server_storage.py frontend\lunatte-v0.2\server.py`
- 启动 8877。
- 检查 `/`、`/api/health`、`/api/stats`、`/api/config`、`/api/moments`、`/api/timeline`。
- 浏览器 smoke：Home / Chat / Archive / Timeline / More / Moments。

产出：

- 不提交。
- 确认当前工作区只有用户自己的未跟踪文件时，不纳入本轮。

#### Phase 1：API 调用清单

状态：已完成。

当前结论：

- `script.js` 中没有 `fetchJson(...)`；真实调用来源是 `appApi.*` 和裸 `fetch(...)`。
- Home、Chat、Config、Moments、Timeline、Wake、Archive、Profile Assets 的 API 调用已按路径、method、body 来源和消费函数完成清点。
- Phase 2/3 以真实调用清单执行，不按不存在的 `fetchJson(...)` 机械替换。

只读扫描：

- 找出 `script.js` 中所有 `fetchJson(...)` 调用。
- 按页面归类：Home、Chat、Config、Moments、Timeline、Wake、Archive、Profile Assets。
- 记录每个调用的 path、method、body 来源、消费返回值的函数。

产出：

- 不新增文档。
- 直接作为实现依据。

#### Phase 2：创建 `shared/lunatte-api.js`

状态：已完成。

已落地：

- 新增 `frontend/lunatte-v0.2/shared/lunatte-api.js`。
- `window.LunatteApi = { createClient }` 已暴露。
- 现有 `requestJson` / `getJson` / `postJson` 能力已从 core 迁入 API client。
- `/api/*` 路径从 `shared/lunatte-core.js` 移出，集中到 `shared/lunatte-api.js`。
- `index.html` 已按 `lunatte-core.js`、`lunatte-api.js`、后续 `lunatte-state.js`、`script.js` 的顺序预留加载关系。

实现：

- 新建 API client 文件。
- 复用或迁移现有 `fetchJson` 能力。
- 按 Phase 1 清单建立语义化 API 函数。
- 通过 `window.LunatteApi = {...}` 暴露，保持当前非打包、非 ESM 模式。

`index.html`：

- 在 `script.js` 前加载 `shared/lunatte-api.js`。
- 加载顺序必须是：
  1. `shared/lunatte-core.js`
  2. `shared/lunatte-api.js`
  3. 后续 `shared/lunatte-state.js`
  4. `script.js`

验收：

- `script.js` 仍能运行。
- 旧 API 返回处理不变。

#### Phase 3：替换 `script.js` 里的 API 调用

状态：已完成。

已落地：

- `script.js` 使用 `window.LunatteApi.createClient({ isOnline })`。
- Home / service status 已改为 `getStats()`、`getHealth()`。
- Config / self / auto comments / Aimas probe 已改为 `getConfig()`、`saveConfig()`、`probeAimas()`。
- Chat wrapper 已继续走 API client。
- Moments 已改为 `getMoments()`、`createMoment()`、`runMomentAction()`、`previewMomentsAutoComments()`。
- Wake 已改为 `getWakeInbox()`、`createWakeDraft()`、`createAutoWakeDraft()`、`updateWakeDraft()`。
- Archive 已改为 `getConfirmedMemory()`、`createDailyMemorySummary()`、`confirmMemory()`、`refreshMemoryIndex()`、`getContext()`、`searchArchive()`。
- Timeline 已改为 `getTimeline()`。
- Profile assets 已改为 `getProfileAssets()`、`saveProfileAsset()`、`clearProfileAssets()`。
- 页面层仍保留 loading、toast、DOM 更新和渲染；API client 只负责 path、method、body 和 JSON 错误口径。

实现：

- 将裸 `fetchJson("/api/...")` 替换成 `LunatteApi.*`。
- 页面层保留 loading、toast、DOM 更新。
- 每完成一组页面调用就跑 `node --check`。

建议顺序：

1. Home / service status。
2. Config / self / auto comments。
3. Chat。
4. Moments。
5. Wake。
6. Archive。
7. Timeline。
8. Profile assets。

验收：

- `script.js` 不再散落 API path 字符串，或只保留少量明确注释的兼容调用。
- API client 负责 path，页面层负责 UI。

#### Phase 4：状态调用清单

状态：已完成。

当前结论：

- `script.js` 只有一个状态入口：`window.LunatteState.createBrowserStorage()`。
- `localStorage` 直连和 `stillgarden.*` key 构造已集中到 `shared/lunatte-state.js`。
- 可直接进入 state 层的内容：chat prefs、browser chat drafts、profile assets fallback、browser moments、moment draft、wake fallback。
- 必须留在 `script.js` 的内容：带 DOM、toast、render、按钮状态、页面刷新协调的包装函数。

只读扫描：

- 找出 `script.js` 中所有 `localStorage` 调用。
- 找出所有 key 构造函数。
- 找出 browser fallback 数据：chat drafts、chat prefs、profile assets、moments、moment draft、wake。
- 标明哪些函数可以直接搬到 state 层，哪些因为直接操作 DOM 必须留在 `script.js`。

产出：

- 不新增文档。
- 直接作为实现依据。

#### Phase 5：创建 `shared/lunatte-state.js`

状态：已完成。

已落地：

- 新增 `frontend/lunatte-v0.2/shared/lunatte-state.js`。
- 通过 `window.LunatteState = { createBrowserStorage }` 暴露。
- key 名称保持不变：`stillgarden.session.*.drafts`、`stillgarden.chat.preferences`、`stillgarden.profile.assets`、`stillgarden.moments.feed`、`stillgarden.moments.draft`、`stillgarden.wake.inbox`。
- 存储格式保持不变：数组/对象结构和 slice 上限沿用原逻辑。
- `shared/lunatte-core.js` 已移除 browser storage 职责。

实现：

- 新建 state 文件。
- 先只搬纯 localStorage 和纯数据计算。
- 通过 `window.LunatteState = {...}` 暴露。

要求：

- key 名称完全不变。
- 存储格式完全不变。
- 浏览器离线 fallback 行为不变。

#### Phase 6：替换 `script.js` 里的状态读写

状态：已完成。

已落地：

- `script.js` 不再直接引用 `localStorage`。
- chat prefs 纯读写委托给 `LunatteState`，DOM 刷新仍留在页面层。
- profile assets fallback、moment draft、browser moments、wake fallback、browser chat drafts 通过 `browserStore` 调用 state 层。
- 页面层仍保留 DOM 渲染、事件绑定、toast 和写入失败提示。

实现：

- 将 localStorage 直连替换为 `LunatteState.*`。
- 页面层仍保留 DOM 渲染和事件绑定。
- 替换顺序先低风险后高风险：
  1. chat prefs。
  2. profile assets fallback。
  3. moment draft。
  4. browser moments fallback。
  5. browser wake fallback。
  6. browser chat drafts。

验收：

- `script.js` 中 localStorage 直连显著减少。
- fallback 行为仍可用。

#### Phase 7：回归与提交

状态：已完成。

已完成检查：

- `node --check`：`lunatte-core.js`、`lunatte-api.js`、`lunatte-state.js`、`script.js`。
- `python -m py_compile`：`server_config.py`、`server_storage.py`、`server.py`。
- 短暂启动 8877 并确认 `/`、`/api/health`、`/api/stats`、`/api/config`、`/api/moments`、`/api/timeline` 均返回 200。
- 浏览器 smoke：Home / Chat / Archive / Timeline / More / Moments。
- 已确认 8877 关闭。
- 已确认无 `sessions/prototype` diff、无 CSS diff、无 `server.py` diff。

必须检查：

- `node --check frontend\lunatte-v0.2\shared\lunatte-core.js`
- `node --check frontend\lunatte-v0.2\shared\lunatte-api.js`
- `node --check frontend\lunatte-v0.2\shared\lunatte-state.js`
- `node --check frontend\lunatte-v0.2\script.js`
- `python -m py_compile frontend\lunatte-v0.2\server_config.py frontend\lunatte-v0.2\server_storage.py frontend\lunatte-v0.2\server.py`
- 短暂启动 8877。
- 检查 `/`、`/api/health`、`/api/stats`、`/api/config`、`/api/moments`、`/api/timeline`。
- 浏览器 smoke：Home / Chat / Archive / Timeline / More / Moments。
- 确认 8877 关闭。

必须确认：

- 无 `sessions/prototype` diff。
- 无 CSS diff。
- 无 `server.py` diff。
- UI 截面不变。
- API 返回格式不变。

提交建议：

```text
split web api and state layers
```

### 成功标准

- 新增 `shared/lunatte-api.js`。
- 新增 `shared/lunatte-state.js`。
- `script.js` 明显变薄。
- API path 集中在 API client。
- localStorage / browser fallback 状态集中在 state 层。
- DOM 渲染和事件绑定仍留在 Web 层。
- Home / Chat / Archive / Timeline / More / Moments smoke 通过。
- `/api/*` 核心接口 200。
- 不改变任何数据文件。

### 失败处理

- 如果某一组替换后 smoke 失败，只回退该组替换，不回退已验证通过的前序模块。
- 如果 API client 引入加载顺序问题，先检查 `index.html` script 顺序，不改业务逻辑。
- 如果 state 替换导致离线 fallback 异常，优先比对 localStorage key 和数据结构，不改 UI。

## Goal R2：页面 Controller 层拆分

R1 完成后立即执行 R2，不等待任何新功能。R2 的目标是把页面 DOM 渲染、事件绑定、页面内状态协调和页面切换逻辑从 `script.js` 抽离到 Web controller 层。`script.js` 最终只负责初始化顺序、全局依赖检查、controller 注册和启动。

状态：已完成。

已落地：

- 新增 `frontend/lunatte-v0.2/web/`，按页面和 Web UI 职责拆出 controller。
- `script.js` 只保留 bootstrapping：依赖检查和 `window.LunatteWebApp.boot()`。
- 页面 DOM 渲染、事件绑定、页面切换、toast/dialog/subpage 控制已迁入 Web controller 层。
- API client 与 state 层继续保留在 `shared/`，未回流到页面 controller。
- 未改 UI、未改 server、未改数据。

### 目标文件

建议新增：

- `frontend/lunatte-v0.2/web/app-bootstrap.js`
- `frontend/lunatte-v0.2/web/navigation-controller.js`
- `frontend/lunatte-v0.2/web/home-controller.js`
- `frontend/lunatte-v0.2/web/rooms-controller.js`
- `frontend/lunatte-v0.2/web/chat-controller.js`
- `frontend/lunatte-v0.2/web/profile-controller.js`
- `frontend/lunatte-v0.2/web/moments-controller.js`
- `frontend/lunatte-v0.2/web/wake-controller.js`
- `frontend/lunatte-v0.2/web/archive-controller.js`
- `frontend/lunatte-v0.2/web/timeline-controller.js`
- `frontend/lunatte-v0.2/web/settings-controller.js`
- `frontend/lunatte-v0.2/web/dialog-controller.js`

允许保留：

- `frontend/lunatte-v0.2/script.js`，但只能作为 bootstrapping 入口。

### 拆分范围

必须拆出：

- Chat 页面渲染、发送流程协调、搜索面板、个人资料页、聊天信息页事件绑定。
- Moments 页面渲染、发布器、评论框、点赞/评论/删除、自动候选按钮事件绑定。
- Archive 搜索、确认记忆列表、记忆卡表单、二次确认、刷新入口。
- Timeline 筛选、分页、展开/收起、详情渲染。
- More / 当前状态 / Provider / Aimas / 自主入口等设置页事件绑定。
- Home / Rooms / 底部导航 / 子页面 appbar。
- 全局 dialog / toast / subpage 这类 Web UI 控制器。

禁止保留在 `script.js`：

- 页面 HTML 拼接。
- DOM query 后的具体渲染。
- 页面按钮事件绑定。
- 页面内业务流程协调。
- localStorage 直接读写。
- `/api/*` 调用。

允许保留在 `script.js`：

- 加载依赖。
- 检查 `window.LunatteCore` / `window.LunatteApi` / `window.LunatteState`。
- 初始化 controller。
- 启动 heartbeat / 首屏 boot 顺序。

### 执行顺序

1. 新建 `web/` 目录和 `app-bootstrap.js`。
2. 先拆无写入或低写入风险 controller：Home、Navigation、Timeline。
3. 再拆中等风险 controller：Archive、Settings、Wake。
4. 再拆高耦合 controller：Chat、Profile、Moments。
5. 最后清理 `script.js`，让它只剩 bootstrapping。

### 验收标准

- `script.js` 只保留初始化入口，目标体量约 200-500 行。
- 所有页面 controller 文件职责清楚。
- 不改变任何 UI、API、数据。
- Home / Rooms / Chat / Archive / Timeline / More / Moments smoke 通过。
- 圈圈发布、聊天发送、Archive 确认记忆这类写入型链路只做手动受控验证，不自动乱写测试数据。

提交建议：

```text
split web page controllers
```

## Goal R3：Python Service 与 Routes 层拆分

R2 完成后立即执行 R3。R3 的目标是消灭单体 `server.py`：业务逻辑进入 service 层，API path 分发进入 routes 层，`server.py` 只保留服务启动、静态文件服务和路由注册。

状态：已完成。

已落地：

- 新增 `frontend/lunatte-v0.2/services/`，按 config、chat、archive、moments、timeline、wake、context、stats、assets 拆出业务服务。
- 新增 `frontend/lunatte-v0.2/providers/`，拆出 OpenAI-compatible、Gemini、Hermes/Aimas 和 HTTP JSON 请求处理。
- 新增 `frontend/lunatte-v0.2/routes/`，由 `routes/router.py` 汇总 GET/POST path 分发表，各 `routes_*.py` 只做 query/body 解析、调用 service、返回 payload/status。
- `server.py` 已降到约 120 行，只保留 `Handler`、静态/asset 响应、`write_json`、router 调用和 `main()`。
- `/api/*` 路径未改，返回结构按原函数迁移。
- 未改 `sessions/prototype` 数据。

已验证：

- `py_compile` 覆盖 `server.py`、`server_config.py`、`server_storage.py`、`services/*.py`、`providers/*.py`、`routes/*.py`。
- 短暂启动 8877，`/`、`/api/health`、`/api/stats`、`/api/config`、`/api/search`、`/api/session-log`、`/api/wake-inbox`、`/api/moments`、`/api/moments-auto-preview`、`/api/timeline`、`/api/profile-assets`、`/api/memory-confirmed`、`/api/context` 均返回 200。
- `/api/health` 返回的数据路径是 `D:\A月亮啊\lunette\sessions\prototype`。
- 已确认 `sessions/prototype` 无 diff。

### 目标结构

建议新增：

```text
frontend/lunatte-v0.2/
  services/
    __init__.py
    config_service.py
    chat_service.py
    archive_service.py
    moments_service.py
    timeline_service.py
    wake_service.py
    context_service.py
    stats_service.py
  providers/
    __init__.py
    openai_provider.py
    gemini_provider.py
    hermes_provider.py
  routes/
    __init__.py
    router.py
    routes_config.py
    routes_chat.py
    routes_archive.py
    routes_moments.py
    routes_timeline.py
    routes_wake.py
    routes_assets.py
    routes_context.py
    routes_stats.py
```

### Service 层必须拆出

- `archive_service.py`
  - confirmed-memory 读取。
  - memory candidate 生成。
  - confirm memory。
  - 后续 edit / revoke / delete / audit 事件模型入口。

- `timeline_service.py`
  - timeline event 映射。
  - session / moments / confirmed-memory 聚合。
  - 时间戳解析、排序、筛选准备。

- `moments_service.py`
  - moments 事件流读取。
  - create / update moment。
  - comment / like / delete。
  - auto preview、冷却、安静时段。
  - generate moment comment 的业务协调。

- `chat_service.py`
  - session log 读写。
  - chat package。
  - provider model 调用协调。
  - Aimas / Hermes chat 协调。
  - 回复清洗、复读护栏、fallback。

- `wake_service.py`
  - wake inbox。
  - manual / auto wake draft。
  - wake action。

- `context_service.py`
  - memory_context 调用。
  - context preview。
  - search metadata。

- `stats_service.py`
  - 本地统计。

- `config_service.py`
  - config read/write、provider secret、self access、moments auto config。

### Provider 层必须拆出

- OpenAI-compatible 请求构造和解析。
- Gemini 请求构造和解析。
- Hermes / Aimas 请求构造和解析。
- provider URL / model / API key 处理。

### Routes 层必须拆出

必须把 `Handler.do_GET` / `Handler.do_POST` 中的巨型 if/elif 路由分发拆出：

- `routes/router.py` 负责 path -> route handler 分发表。
- `routes_*.py` 只负责：
  - 解析 query/body。
  - 调用 service。
  - 返回 payload/status。
- route 层不能直接做复杂业务逻辑。
- route 层不能直接读写 JSONL，必须走 service/storage。

### `server.py` 最终允许保留

- `Handler` 基类。
- 静态文件响应。
- assets 文件响应。
- 调用 routes router。
- `write_json`。
- `main()` 启动服务。

禁止保留：

- provider 调用逻辑。
- moments/timeline/archive/chat 业务逻辑。
- 巨型 `/api/*` if/elif。
- JSONL 读写。
- config 业务规则。

### 执行顺序

1. 先建 `services/`、`providers/`、`routes/` 空结构和最小 `__init__.py`。
2. 拆 config / stats / context 这类低风险服务。
3. 拆 archive / timeline 只读或追加型服务。
4. 拆 wake / moments。
5. 拆 chat / providers。
6. 拆 routes 分发表。
7. 清理 `server.py`。

### 验收标准

- `server.py` 目标体量降到约 300-600 行。
- 所有现有 `/api/*` 路径不变。
- 所有返回格式不变。
- `sessions/prototype` 无 diff，除非手动测试明确执行写入链路。
- `/api/health` 仍返回 V0.2 数据路径。
- `/`、`/api/stats`、`/api/config`、`/api/moments`、`/api/timeline` 全部 200。

提交建议：

```text
split server services and routes
```

## Goal R4：视图层与 CSS 最后清理

R3 完成后执行 R4。R4 负责清理 HTML 结构和超大 CSS 模块，不改变 UI。

状态：已完成。

已落地：

- `index.html` 增加页面、共享弹层、底部导航、脚本区注释；未改 DOM id、class、文本和页面结构语义。
- `css/20-chat-profile-dialogs.css` 已拆为 `css/20-chat.css`、`css/21-profile-dialogs.css`、`css/22-chat-search.css`。
- `styles.css` 继续只作为 CSS 入口。
- `00-tokens.css` 继续保留主题 token。
- `50-moments-wake.css` 当前约 741 行，未超过 1000 行，本轮不拆。

已验证：

- `py_compile` 覆盖后端入口和拆分后的 service/provider/routes。
- `node --check` 覆盖 shared、web controllers 和 `script.js`。
- `/`、`/styles.css`、`/css/20-chat.css`、`/css/21-profile-dialogs.css`、`/css/22-chat-search.css`、`/api/health`、`/api/stats`、`/api/config`、`/api/moments`、`/api/timeline` 均返回 200。
- 浏览器 smoke：Home / Rooms / Chat / Archive / Timeline / More / Moments 可切换，无 console error/warning。
- 浏览器 CSS import 检查已看到 `20-chat.css`、`21-profile-dialogs.css`、`22-chat-search.css`。

### `index.html` 结构化清理

允许：

- 增加清晰注释区块。
- 按页面/模板/弹层整理已有 HTML 段落。
- 调整 script 加载顺序以匹配 R1/R2。

禁止：

- 不改变 DOM id。
- 不改变 class。
- 不改变文本。
- 不改变页面结构语义。
- 不删除占位功能。

### CSS 拆分

必须拆：

- `20-chat-profile-dialogs.css` 超过 1000 行，拆成：
  - `20-chat.css`
  - `21-profile-dialogs.css`
  - `22-chat-search.css`

视情况拆：

- `50-moments-wake.css` 如果 R2/R3 后继续变大，拆成：
  - `50-moments.css`
  - `51-wake.css`

要求：

- `styles.css` 继续只作为 CSS 入口。
- 主题 token 继续在 `00-tokens.css`。
- 不为主题复制页面 CSS。
- 不改变视觉。

### 验收标准

- HTML 可读性提升。
- 超过 1000 行的 CSS 模块被拆分。
- 页面视觉不变。
- Home / Rooms / Chat / Archive / Timeline / More / Moments smoke 通过。

提交建议：

```text
clean html and split large css modules
```

## 剩余拆分计划

当前不是“按需再拆”，而是 R1-R4 作为当前阶段的完整重构清算队列，必须在任何新功能前完成。目标是避免继续把新功能堆进 `script.js` / `server.py`，同时避免为了形式拆出没有职责的空模块。

## R5：全局维护架构治理方案

R1-R4 已经把最大单体文件拆开，但“拆开”不等于“长期好维护”。下一阶段必须补一层全局治理：让主题、组件、页面、业务、路由、数据各有固定写入位置，避免后续继续在拆分后的文件里堆出新的大泥团。

### 当前架构判断

已解决：

- `script.js` 已降级为 bootstrapping。
- Web DOM 已进入 `web/*-controller.js`。
- API client / browser state 已进入 `shared/`。
- `server.py` 已降级为 HTTP/static 入口。
- 后端业务已进入 `services/`。
- provider 请求已进入 `providers/`。
- API path 分发已进入 `routes/`。
- `20-chat-profile-dialogs.css` 已拆为 chat、profile/dialog、chat-search。

仍不够理想：

- CSS 现在是“按页面/功能拆”，但缺少统一的 component/primitives 层。全局按钮、输入框、卡片、开关、弹层、头像、toast、状态条如果继续分散在页面 CSS，主题化仍会变成逐模块重写。
- `web/moments-controller.js`、`web/chat-controller.js`、`web/settings-controller.js` 已接近或超过 700 行，后续新增功能前要继续拆出 controller helpers 或 feature controllers。
- `services/moments_service.py`、`services/chat_service.py`、`services/config_service.py` 已是 300-400 行级别，后续不能继续把生成、解析、策略、写入全部塞在一个 service 文件里。
- 配置目前混合了 provider secret、UI setting、self access、moments auto config。还能运行，但后续要区分 public config、secret config、feature policy。
- JSONL 仍是原型数据层；后续 SQLite/FTS5 不能直接让 route/controller 访问数据库，必须先经过 repository/service。

### 完美架构目标

目录目标不是为了“好看”，而是为了让每次新增功能都能回答一个问题：这段代码应该写在哪里，谁负责全局样式，谁负责页面差异，谁负责业务规则，谁负责 IO。

```text
frontend/lunatte-v0.2/
  styles.css                    # CSS 入口，只 import，不写样式
  css/
    00-tokens.css               # 设计 token：颜色、字体、圆角、阴影、间距、z-index
    01-theme-base.css           # 可选：主题语义变量映射，不写页面选择器
    05-components.css           # 通用组件：按钮、输入、卡片、开关、弹层、toast、头像、状态条
    10-layout-shell.css         # app shell / phone shell / dock / responsive
    20-chat.css                 # Chat 页面特有布局
    21-profile.css              # Profile 页面特有布局
    22-dialogs.css              # 通用 app dialog / confirm sheet
    23-chat-search.css          # Chat 搜索和上下文预览
    30-archive.css              # Archive 特有布局
    31-timeline.css             # Timeline 特有布局
    40-settings.css             # More / 当前状态 / provider 设置
    50-moments.css              # Moments 特有布局
    51-wake.css                 # Wake 特有布局

  shared/
    lunatte-core.js             # 纯数据/格式化/常量
    lunatte-api.js              # API client
    lunatte-state.js            # browser storage / local state

  web/
    app-bootstrap.js            # Web 启动和 controller 注册
    controllers/                # 未来可选：按页面再拆一层目录
    views/                      # 未来可选：纯 HTML render 函数
    interactions/               # 未来可选：事件绑定/手势/菜单等 Web 专用逻辑

  services/
    *_service.py                # 业务用例，不直接写 HTTP
  repositories/
    *_repository.py             # 未来数据层：JSONL / SQLite / assets IO
  providers/
    *_provider.py               # 外部模型/agent HTTP
  routes/
    routes_*.py                 # HTTP query/body 解析 + service 调用
```

### CSS 写入规则

全局样式只能从上往下流，不允许页面模块各自复制一套。

- 改整体气质、颜色、字体、圆角、阴影、间距：只改 `00-tokens.css` 或未来 `01-theme-base.css`。
- 改所有按钮、输入框、开关、卡片、弹层、toast、头像、状态条：进 `05-components.css`，不要分散到页面 CSS。
- 改 app shell、手机壳、底部导航、响应式：进 layout/responsive 层，不进页面 CSS。
- 改 Chat 独有布局：进 `20-chat.css`。
- 改 Profile 独有布局：进 `21-profile.css`。
- 改 Dialog / Confirm 通用弹层：进 `22-dialogs.css`。
- 改 Chat 搜索：进 `23-chat-search.css`。
- 改 Archive / Timeline：分别进 `30-archive.css` / `31-timeline.css`。
- 改 Moments / Wake：分别进 `50-moments.css` / `51-wake.css`。
- 页面 CSS 不允许新增硬编码色值；除非是新 token 落地前的临时标记，提交前必须回收到 token。

CSS 拆分阈值：

- 任一 CSS 文件超过 700 行：必须审查是否混入多个功能。
- 任一 CSS 文件超过 1000 行：必须拆分后才能继续新功能。
- 同一类按钮/输入/卡片样式在 3 个文件重复出现：必须抽到 `05-components.css`。

### 前端 JS 写入规则

- `script.js` 永远只做 bootstrapping。
- `shared/` 只能放纯逻辑、API client、browser state，不能访问 DOM。
- `web/*-controller.js` 可以访问 DOM，但不能直接拼 API path；必须走 `LunatteApi`。
- controller 超过 700 行：新增功能前先拆 `views`、`actions` 或子 controller。
- render 函数如果超过 120 行或重复出现 3 次，拆成 view helper。
- 事件绑定只能在 controller/init 层；纯数据计算要回到 `shared/`。

前端下一步优先级：

1. `web/moments-controller.js` 拆出 render/helpers/actions。
2. `web/chat-controller.js` 拆出 render/search/profile action。
3. `web/settings-controller.js` 拆出 self access、auto reply、provider config 三组 controller。

### 后端写入规则

- `server.py` 不再写业务逻辑。
- `routes/*.py` 只做 HTTP 解析、调用 service、返回 payload/status。
- `services/*.py` 写业务规则，但不直接关心 HTTP。
- `providers/*.py` 只负责外部模型/agent 请求和响应解析。
- 未来新增 `repositories/` 后，JSONL / SQLite / assets IO 必须进 repository；service 不能到处打开文件。
- `server_config.py` 只放路径、默认值、轻量 normalize；复杂业务配置规则移入 `config_service.py`。
- `server_storage.py` 只保留通用读写工具，不承载某个功能的业务规则。

后端拆分阈值：

- 任一 service 超过 500 行：新增功能前拆出 policy/helper/repository。
- 同一 service 同时处理“生成 prompt + 调 provider + 写数据 + 读列表”四件事：必须拆。
- 任何 route 中出现 JSONL / SQLite / provider 调用：必须退回 service/provider。
- 任何 provider 里出现 UI 文案或页面状态：必须退回 service。

后端下一步优先级：

1. 新建 `repositories/`，把 session/moments/archive/wake/profile assets 的 JSONL/asset IO 从 service 中再下沉。
2. 拆 `moments_service.py`：events repository、auto policy、comment generation。
3. 拆 `chat_service.py`：session repository、reply policy、provider orchestration。
4. 拆 `config_service.py`：public config、secret config、feature policies。

### 配置与数据层规则

配置分三类：

- Public config：前端可以读取，例如 room label、provider model 名、self access 状态。
- Secret config：API key、agent key，只能通过 secret storage，不返回明文。
- Feature policy：self access、moments auto、quiet time、cooldown、后续 wake scheduler policy。

数据分三层：

- Prototype JSONL：当前运行基准，继续保护，不能随手迁移。
- Repository API：下一步给 JSONL 包一层读写接口，为 SQLite 迁移做准备。
- SQLite / FTS5：后续事件索引，不直接替代原始 JSONL，先做副本索引和审计模型。

数据库原则：

- 先 append-only event model，再做 edit/revoke/delete audit。
- SQLite 先做查询索引，不要一开始就成为唯一真源。
- 任何迁移都必须用 `sessions/prototype` 副本验证，不能直接写 V0.1 原始数据。

### 下一阶段建议：R5 执行顺序

R5 不是新业务，是“维护架构补强”。

1. CSS R5.1：新增 `05-components.css`，抽通用按钮、输入、开关、卡片、弹层、toast、头像、状态条。
2. CSS R5.2：继续拆 `30-archive-timeline.css` 为 Archive / Timeline。
3. CSS R5.3：继续拆 `50-moments-wake.css` 为 Moments / Wake。
4. CSS R5.4：把 `21-profile-dialogs.css` 拆为 profile / dialogs，避免资料页和通用弹层继续混在一起。
5. JS R5.5：拆 `moments-controller.js`。
6. JS R5.6：拆 `chat-controller.js`。
7. Backend R5.7：新增 repositories，先下沉 JSONL IO。
8. Backend R5.8：拆 `moments_service.py` 和 `chat_service.py` 的 policy/provider orchestration。

R5 验收标准：

- 全局视觉改动主要通过 tokens/components 完成，不需要逐页面改。
- 页面 CSS 只保留页面布局和局部差异。
- controller 单文件不超过 700 行，超过则有明确拆分计划。
- service 单文件不超过 500 行，超过则有 repository/policy/helper 拆分计划。
- 新功能恢复前，必须明确落到对应模块，不允许回填到入口文件或最大文件。

### 日常提交规则

- 结构重构提交和业务功能提交分开。
- 写入型测试产生的 `sessions/prototype` diff 默认不提交，除非用户明确要求保留为种子数据。
- 每次提交前跑：
  - `python -m py_compile frontend\lunatte-v0.2\server.py ...`
  - `node --check frontend\lunatte-v0.2\script.js` 和相关 controller/shared 文件。
  - 短暂启动 8877，检查 `/api/health` 数据路径。
  - 至少 smoke Home / Rooms / Chat / Archive / Timeline / More / Moments。
- 文档必须同步当前结构，不允许 README / plan 继续引用已删除文件作为当前文件。

### 当前体量

- `script.js`：约 11 行，已降级为 bootstrapping 入口。
- `frontend/lunatte-v0.2/web/`：当前承接 Web DOM controller，是 R2 后续维护入口。
- `shared/lunatte-core.js`：约 880 行，已承接 Web/App 可复用底层。
- `server.py`：约 120 行，已只保留 HTTP/static 入口、asset 响应、router 调用、`write_json` 和启动逻辑。
- `services/`：当前承接 config、chat、archive、moments、timeline、wake、context、stats、assets 业务。
- `providers/`：当前承接 OpenAI-compatible、Gemini、Hermes/Aimas 和 HTTP JSON 请求处理。
- `routes/`：当前承接 API path 分发表和 route 层 query/body 解析。
- `server_config.py`：约 180 行，边界清楚，暂不需要继续拆。
- `server_storage.py`：约 180 行，边界清楚，暂不需要继续拆。
- 当前没有超过 1000 行的 CSS 模块；`50-moments-wake.css` 约 741 行，暂不拆。

### 拆分原则

- 新功能不能继续直接塞进大文件的任意位置；先找对应模块，没有模块就先建最小真实模块。
- 每次只拆一个功能边界，拆完必须能启动和回归。
- 不抽“只有一两个函数”的空壳模块，除非它解除一个明确依赖。
- 不为了 React Native / App 提前重写 Web 层；只把纯逻辑和数据协议往可复用层移动。
- 不把 DOM 节点、CSS class、toast、弹窗、事件绑定放进 `shared/`。
- `sessions/prototype` 仍是 V0.2 副本数据源，任何拆分都不能改变写入路径或 JSONL 追加语义。

### JS R1/R2 清算明细

优先级：最高。R1/R2 是当前阶段任务，不等待任何前端新功能。

#### JS-1：API client 层

目标文件建议：

- `frontend/lunatte-v0.2/shared/lunatte-api.js`

拆出内容：

- `/api/stats`
- `/api/health`
- `/api/session`
- `/api/chat-package`
- `/api/provider-chat`
- `/api/aimas-chat`
- `/api/config`
- `/api/moments`
- `/api/timeline`
- `/api/wake-inbox`
- `/api/memory/*`
- `/api/profile-assets`

保留在 `script.js`：

- DOM 状态提示。
- toast 文案。
- 按钮 loading 状态。
- 渲染结果。

当前阶段：

- R1 立即执行这一刀，不等待下一个业务功能。

成功标准：

- `script.js` 不再散落裸 `fetchJson(...)` 调用。
- API 路径集中，后续 App 可以复用请求协议。
- 返回格式不变。

#### JS-2：domain state 层

目标文件建议：

- `frontend/lunatte-v0.2/shared/lunatte-state.js`

拆出内容：

- chat prefs。
- profile assets local fallback。
- browser drafts。
- moments draft / local fallback。
- wake local fallback。
- timeline filter / pagination 的纯状态计算。

保留在 `script.js`：

- DOM 读取。
- DOM 写入。
- class 切换。
- 事件绑定。

当前阶段：

- R1 立即执行这一刀，不等待下一个业务功能。

成功标准：

- localStorage key 和读写集中。
- 页面渲染层只调用 state helper，不直接拼 key。
- 不改变浏览器兜底行为。

#### JS-3：页面 controller 分层

目标文件建议：

- `frontend/lunatte-v0.2/web/chat-controller.js`
- `frontend/lunatte-v0.2/web/moments-controller.js`
- `frontend/lunatte-v0.2/web/archive-controller.js`
- `frontend/lunatte-v0.2/web/timeline-controller.js`
- `frontend/lunatte-v0.2/web/settings-controller.js`

拆出内容：

- 只拆 Web DOM controller，不放入 `shared/`。
- 每个 controller 管一个页面的事件绑定、渲染入口和页面内状态。
- `script.js` 最终降成 bootstrapping：加载配置、注册底部导航、初始化 controllers。

当前阶段：

- R2 已完成；后续页面逻辑维护必须优先进入对应 controller，不再回填 `script.js`。

成功标准：

- `script.js` 降级为 bootstrapping。
- 每个页面功能能在对应 controller 内定位。
- Home / Chat / Archive / Timeline / More / Moments smoke 必须全部通过。

### 后端 R3 清算明细

优先级：最高。R3 是当前阶段任务，不等待数据库、Archive 审计、RAG 或自动调度。

#### PY-1：services/archive_service.py

拆出内容：

- `read_confirmed_memory`
- `build_daily_summary_candidate`
- `confirm_memory`
- 后续 edit / revoke / delete / audit 事件模型。

当前阶段：

- R3 必须拆出 archive service，不等待 Archive 编辑功能。

成功标准：

- route 只负责读请求和返回 JSON。
- confirmed-memory 仍只追加 JSONL，不物理删除旧记录。
- 权限字段和 sensitive 字段逻辑集中。

#### PY-2：services/timeline_service.py

拆出内容：

- `timeline_event`
- `read_timeline`
- 时间戳解析和排序。
- 后续 SQLite events schema 映射。

当前阶段：

- R3 必须拆出 timeline service，不等待 SQLite / Timeline 新功能。

成功标准：

- Timeline 只读聚合规则集中。
- session / moments / confirmed-memory 三类来源映射清楚。
- 不迁移、不写入、不替代 Archive。

#### PY-3：services/moments_service.py

拆出内容：

- `read_moments`
- `create_moment`
- `update_moment`
- `read_moments_auto_preview`
- `generate_moment_comment`
- 自动评论冷却和安静时段判断。

当前阶段：

- R3 必须拆出 moments service，不等待自动评论后台触发器或自动发圈。

成功标准：

- moments 事件流读写集中。
- 自动评论候选和确认写入路径一致。
- 不让 route 混入模型调用细节。

#### PY-4：services/chat_service.py 与 providers/

拆出内容：

- `call_provider_model`
- `aimas_chat`
- `call_room_model_for_text`
- OpenAI-compatible / Gemini / Hermes 请求构造。
- 回复清洗和复读护栏。

当前阶段：

- R3 必须拆出 chat service 与 provider adapters，不等待人格/RAG/流式回复。

成功标准：

- provider 适配和住户业务规则分开。
- 人格/RAG 不再散落在 chat、moments、archive 各处。
- 能清楚看到“本次请求送出了什么”。

#### PY-5：routes 分发收敛

目标文件建议：

- 先不急着引入 FastAPI。
- 保持 `SimpleHTTPRequestHandler`，但把 API path 分发表抽成函数或小路由表。

当前阶段：

- R3 必须同步拆 routes 层，不等待新增 endpoint。

成功标准：

- API 路径表可读。
- 返回格式不变。
- 静态文件服务和 API handler 边界清楚。

### CSS / HTML R4 清算明细

优先级：高。R4 是当前阶段任务，必须在恢复新功能前完成。

#### CSS-1：chat/profile 继续拆

目标文件建议：

- `css/20-chat.css`
- `css/21-profile-dialogs.css`
- `css/22-chat-search.css`

当前阶段：

- R4 已完成；后续聊天相关样式分别进入 `20-chat.css`、`21-profile-dialogs.css`、`22-chat-search.css`，不要重建单体文件。

#### CSS-2：moments/wake 继续拆

目标文件建议：

- `css/50-moments.css`
- `css/51-wake.css`

当前阶段：

- R4 已评估：`50-moments-wake.css` 当前约 741 行，未超过 1000 行，暂不拆；后续 Moments/Wake 新增 UI 时再按真实边界拆。

#### CSS-3：主题化

目标：

- 主题只改 `00-tokens.css` 或新增 token override。
- 页面 CSS 不按主题复制。

当前阶段：

- R4 检查 token 命名是否足够支撑后续主题化；如不够，先清理 token，再恢复新功能。

### 暂缓拆分

- 不引入 React / Vue / 打包器。
- 不把所有 JS 一次性改成 ESM import 图。
- 不把 `server.py` 直接迁移到 FastAPI。
- 不创建正式数据库。
- 不拆原始导出和旧记忆库源文件。
- 不为了“干净架构”移动 `sessions/prototype`。

### 当前执行顺序

1. R1：拆出 Web API client 与 domain state。
2. R2：拆出页面 controllers，掏空 `script.js`。
3. R3：拆出 Python services、providers、routes，掏空单体 `server.py`。
4. R4：结构化 `index.html`，拆分超大 CSS 模块。
5. R1-R4 全部验证并提交后，才恢复 SQLite、Archive 编辑、打字机效果等新功能。

## 重构完成后的冻结功能池

以下任务不是当前下一步。它们必须等 R1-R4 全部完成后再恢复优先级。

### 1. 设计 SQLite + FTS5 事件索引草图

重构完成后为什么做：

- 当前 `/api/timeline` 已经把聊天、圈圈、确认记忆聚合成只读事件格式。
- 后续 RAG、时间线和搜索需要统一事件索引，但不能直接迁移或替换 JSONL。

涉及文件：

- `frontend/lunatte-v0.2/server.py`
- `sessions/prototype/*.jsonl`
- `sessions/prototype/outbox/*.jsonl`
- `docs/imported-antigravity/问题改进方案/data-structure-and-processing-analysis.md`
- 根目录记忆库相关文档

不允许动哪些文件：

- 不改 `server.py`
- 不改任何 JSONL 数据
- 不创建正式数据库
- 不删除 CSV/JSONL

怎么测试：

- 只设计 schema 草图。
- 对照当前 Timeline 事件字段，列出最小表：events、actors、sources、permissions、attachments。
- 标注哪些字段来自 session、moments、confirmed-memory。

成功标准：

- 有一份 SQLite + FTS5 草图。
- 明确 JSONL 是迁移源和回滚源。
- 明确第一版只做索引层，不替换原始数据。

### 2. Archive 确认记忆编辑/撤回/删除/审计方案

重构完成后为什么做：

- Archive 已经能写入正式确认记忆，但还缺编辑、撤回、删除和权限审计。
- 这是比大拆 UI 更贴近实际使用的数据安全任务。

涉及文件：

- `frontend/lunatte-v0.2/README.md`
- `sessions/prototype/confirmed-memory.jsonl`
- `sessions/prototype/config.json`
- `docs/lunette-v0.2-plan-reviewed.md`
- 根目录 `memory-vault-usage-v0.1.md`

不允许动哪些文件：

- 不改 `confirmed-memory.jsonl`
- 不改 `script.js`
- 不改 `server.py`
- 不改 V0.1 原始数据

怎么测试：

- 只做方案设计。
- 设计事件追加式数据模型：新增 revision / revoked / deleted 标记，而不是物理删除原记录。
- 设计前端入口和二次确认规则。
- 设计审计显示字段：谁可读、是否 sensitive、何时创建、何时撤回。

成功标准：

- 有可执行的数据模型和 UI 入口方案。
- 明确删除不是直接删文件。
- 明确权限审计如何显示和测试。

### 3. 低风险功能池

- 聊天搜索空状态、服务灯文案、圈圈评论样式、三点菜单小动效。
- 请求预览 UI 化卡片。
- Timeline 事件详情继续补字段，但不迁移数据。

## 每轮固定检查

- `python -m py_compile frontend\lunatte-v0.2\server_config.py frontend\lunatte-v0.2\server_storage.py frontend\lunatte-v0.2\server.py`
- `node --check frontend\lunatte-v0.2\script.js`
- 短暂启动 `8877`。
- 检查 `/`、`/api/health`、`/api/stats`、`/api/config`、`/api/moments`、`/api/timeline`。
- 浏览器 smoke：Home / Chat / Archive / Timeline / More / Moments。
