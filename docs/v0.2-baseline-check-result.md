# Lunette V0.2 Baseline Check Result

检查时间：2026-06-29  
工作区：`D:\A月亮啊\lunette-v0.2`  
检查基准 commit：`8c48ae1`  
本轮目标：建立 V0.2 baseline 验证清单，并实际跑一次基础验证。

## 1. 目录纪律检查

命令：

```powershell
git rev-parse --show-toplevel
git status --short --branch
git log --oneline -1
```

结果：

```text
D:/A月亮啊/lunette-v0.2
## main...origin/main
8c48ae1 align v0.2 source documents and next actions
```

结论：

- Git 根目录确认是 `D:\A月亮啊\lunette-v0.2`。
- 执行前工作树干净。
- 本轮未在 V0.2 根目录外创建文件。

## 2. 读取任务来源

已读取：

- `docs/v0.2-next-actions.md` 第 1 个任务：建立 V0.2 基线验证清单。
- `frontend/stillgarden-prototype-v0.1/README.md`。
- `frontend/stillgarden-prototype-v0.1/start-stillgarden.ps1`。
- `frontend/stillgarden-prototype-v0.1/start-stillgarden.bat`。
- `frontend/stillgarden-prototype-v0.1/server.py` 启动入口相关代码。

结论：

- `frontend/stillgarden-prototype-v0.1` 是当前可运行原型层。
- 默认服务端口是 `8877`。
- `server.py` 支持 `STILLGARDEN_NO_OPEN=1`，可用于自动化验证时禁止自动打开浏览器。

## 3. Python 语法检查

命令：

```powershell
python -m py_compile frontend\stillgarden-prototype-v0.1\server.py
```

结果：

```text
通过，退出码 0。
```

结论：

- `server.py` 当前 Python 编译检查通过。

## 4. JS 语法检查

命令：

```powershell
node --check frontend\stillgarden-prototype-v0.1\script.js
```

结果：

```text
通过，退出码 0。
```

结论：

- `script.js` 当前 JavaScript 语法检查通过。

## 5. V0.2 数据副本检查

检查路径：

```text
sessions/prototype
sessions/prototype/config.json
sessions/prototype/confirmed-memory.jsonl
sessions/prototype/moments.jsonl
sessions/prototype/linxu.jsonl
sessions/prototype/dengdeng.jsonl
sessions/prototype/aimas.jsonl
sessions/prototype/outbox
```

结果：

```text
sessions/prototype                          exists, dir
sessions/prototype/config.json              exists, file
sessions/prototype/confirmed-memory.jsonl   exists, file
sessions/prototype/moments.jsonl            exists, file
sessions/prototype/linxu.jsonl              exists, file
sessions/prototype/dengdeng.jsonl           exists, file
sessions/prototype/aimas.jsonl              exists, file
sessions/prototype/outbox                   exists, dir
```

结论：

- V0.2 有自己的 `sessions/prototype` 副本。
- 本轮未写入、修改或删除任何数据文件。
- 后续测试必须继续确认读写路径指向 `D:\A月亮啊\lunette-v0.2\sessions\prototype`。

## 6. 服务启动检查

启动方式：

```powershell
cd D:\A月亮啊\lunette-v0.2\frontend\stillgarden-prototype-v0.1
$env:STILLGARDEN_PORT = "8877"
$env:STILLGARDEN_NO_OPEN = "1"
python .\server.py
```

实际执行方式：

- 使用 PowerShell 启动一个短生命周期 Python 进程。
- 设置 `STILLGARDEN_PORT=8877`。
- 设置 `STILLGARDEN_NO_OPEN=1`，避免自动打开浏览器。
- HTTP 检查完成后立即停止该进程。

只读 HTTP 检查结果：

| Check | URL | Status | Bytes |
|---|---|---:|---:|
| home | `/` | 200 | 47400 |
| api-stats | `/api/stats` | 200 | 301 |
| api-config | `/api/config` | 200 | 2141 |
| api-moments | `/api/moments` | 200 | 4063 |
| api-timeline | `/api/timeline` | 200 | 47155 |

服务关闭后检查：

```text
No listener on 8877 after baseline service test.
```

结论：

- 服务可以启动。
- 首页可以通过 HTTP 打开。
- 基础只读 API 可以响应。
- 本次检查没有留下长期占用 `8877` 的服务。

## 7. 本轮未跑的检查

未跑：

- 浏览器逐页人工点击 Home / Chat / Moments / Archive / Timeline / Profile / More。
- 真实模型调用。
- 写入型操作。
- 图片上传。
- Archive 新增确认记忆。
- Moments 发动态或评论写入。

原因：

- 本轮目标是建立 baseline 清单并跑基础验证。
- 用户明确禁止改代码、动数据。
- 写入型功能不应在 baseline 自动检查中直接执行。

处理方式：

- 这些项目已写入 `docs/v0.2-baseline-verification.md`，作为后续人工回归清单。

## 8. 当前 Baseline Known Issues

当前 baseline 已知问题或未完成功能：

- `styles.css`、`script.js`、`server.py` 仍是单体文件，后续需要拆分。
- Timeline 是只读草稿层，不迁移、不写入、不替代 Archive。
- Moments 自动评论有候选和确认链路，但没有后台自动触发器。
- Wake 有候选和收件箱，但没有真正自主唤醒、通知和调度器。
- Archive 的“每日回声”“下个月再拆开的信”仍是占位。
- Archive 确认记忆还没有编辑、撤回、删除和权限审计。
- Chat 还没有进房间前预热上下文。
- 跨房间搜索、搜索分页、精细命中高亮还没完成。
- Aimas 能聊天，但“小电脑”工具输出折叠卡和完整 Hermes session 管理还没做。
- 图片缩略图、多图、失败队列、回收站还没做。
- SQLite + FTS5、向量库、RAG 还未接入。
- Cloudflare/VPS/CORS/远程部署暂缓。
- UI 模板只作为参考，不能直接合并。
- AGY/Gemini 补丁脚本禁止执行。

## 9. 是否可以进入下一步

结论：

- Python 检查通过。
- JS 检查通过。
- 服务可启动，并能响应首页和基础只读 API。
- V0.2 数据副本存在。
- 本轮未改代码、未动数据。

下一步可以开始：

```text
CSS 拆分边界梳理
```

限制：

- 下一步仍应先做只读边界梳理。
- 不应立即拆 CSS。
- 不应合并 AGY CSS。
- 不应改 `index.html`、`styles.css`、`script.js`、`server.py`，除非用户明确进入对应执行任务。
